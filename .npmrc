# @dentvega scope resolves from GitHub Packages (the shared @dentvega/miniapp-contract).
# Needs a token with read:packages in the GITHUB_TOKEN env (locally and in the Vercel build).
@dentvega:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
