# Audit log tamper-evidente — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Registrar quién hizo qué en el control-plane (publishes + mutaciones) en un log append-only tamper-evidente, con atribución real por-persona.

**Architecture:** Hash-chain append-only por cadena (una por miniapp + `_global`) sobre KV. Append atómico cross-key vía CAS+Lua (mismo patrón que `mutateApp`). Lectura mergeada para el feed. Acceso admin (`/audit`) + maintainer (panel por-miniapp).

**Tech Stack:** Next.js (App Router, esta versión con breaking changes — leer `node_modules/next/dist/docs/` antes de tocar rutas/páginas), TypeScript, Upstash Redis (`@upstash/redis`), `node:crypto` (sha256), Vitest.

**Spec:** `docs/superpowers/specs/2026-09-12-audit-log-design.md`

## Global Constraints

- **Rama:** `feat/audit-log` (ya creada). No commitear a `main`.
- **Sin secretos en `details`:** nunca llaves privadas ni tokens. `set-public-key` guarda solo `hasKey: boolean`.
- **base64url** para hashes (consistente con el resto del proyecto): sha256 → base64 → `+/`→`-_`, sin `=`.
- **`MAX_RETRIES=15`** y `backoffMs`/jitter idénticos a `lib/registry/kv.ts` para el retry del append.
- **Append fire-and-forward:** si `recordAudit` falla tras reintentos, se loguea (`console.error`) y **no** se revierte ni se falla la request de negocio. El audit es secundario a la acción ya cometida.
- **Actor:** UI → `{type:"user",login}` desde `session.githubLogin`; CI → `{type:"ci",login:github.actor||"ci",commit,repo}`.
- **Acceso:** admin = `canScaffold(login, auditAdminLogins())`; por-miniapp = `canManageMiniapp(login, rec.maintainers, auditAdminLogins())` (ambos ya existen en `lib/scaffold-authz.ts`).
- **Cadena `_global`:** para acciones no ligadas a una miniapp (`seed`).
- **Next 16 / next-auth:** importar `auth` de forma lazy dentro del handler (`const { auth } = await import("@/auth")`), como hacen las rutas existentes.

---

## Task 1: KvClient — primitivos de lista + append atómico

**Files:**
- Modify: `lib/registry/kv.ts` (interface `KvClient`, `inMemoryKvClient`, `upstashClient`)
- Test: `lib/registry/__tests__/kv-audit.test.ts` (create)

**Interfaces:**
- Produces:
  - `KvClient.lpush(key: string, value: string): Promise<void>`
  - `KvClient.lrange(key: string, start: number, stop: number): Promise<string[]>`
  - `KvClient.casAppend(headKey: string, listKey: string, expectedHead: string | null, newHead: string, entry: string): Promise<boolean>` — atómico: si `GET(headKey)===expectedHead` (o `expectedHead===null` y no existe), hace `LPUSH(listKey, entry)` + `SET(headKey, newHead)` y devuelve `true`; sino `false`.

- [ ] **Step 1: Write the failing test**

Create `lib/registry/__tests__/kv-audit.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { inMemoryKvClient } from "../kv";

describe("KvClient audit primitives (in-memory)", () => {
  it("lpush prepends and lrange reads newest-first", async () => {
    const kv = inMemoryKvClient();
    await kv.lpush("l", "a");
    await kv.lpush("l", "b");
    expect(await kv.lrange("l", 0, -1)).toEqual(["b", "a"]);
    expect(await kv.lrange("l", 0, 0)).toEqual(["b"]);
  });

  it("casAppend succeeds only when head matches, atomically prepending + advancing head", async () => {
    const kv = inMemoryKvClient();
    // first append: expected head null (absent)
    expect(await kv.casAppend("h", "l", null, "H1", "e1")).toBe(true);
    expect(await kv.get("h")).toBe("H1");
    expect(await kv.lrange("l", 0, -1)).toEqual(["e1"]);
    // stale expected head → rejected, no mutation
    expect(await kv.casAppend("h", "l", "STALE", "H2", "e2")).toBe(false);
    expect(await kv.lrange("l", 0, -1)).toEqual(["e1"]);
    // correct expected head → appended
    expect(await kv.casAppend("h", "l", "H1", "H2", "e2")).toBe(true);
    expect(await kv.lrange("l", 0, -1)).toEqual(["e2", "e1"]);
    expect(await kv.get("h")).toBe("H2");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run lib/registry/__tests__/kv-audit.test.ts`
Expected: FAIL — `kv.lpush is not a function`.

- [ ] **Step 3: Extend the `KvClient` interface**

In `lib/registry/kv.ts`, add to the `KvClient` interface (after `mget`):

```ts
  /** Prepend a value to a list (LPUSH). */
  lpush(key: string, value: string): Promise<void>;
  /** Read a list range, newest-first (LRANGE). stop=-1 → hasta el final. */
  lrange(key: string, start: number, stop: number): Promise<string[]>;
  /**
   * Append atómico cross-key para el hash-chain: si GET(headKey)===expectedHead
   * (o expectedHead===null y no existe), LPUSH(listKey, entry) + SET(headKey, newHead)
   * y devuelve true; sino false.
   */
  casAppend(
    headKey: string,
    listKey: string,
    expectedHead: string | null,
    newHead: string,
    entry: string,
  ): Promise<boolean>;
```

- [ ] **Step 4: Implement in `inMemoryKvClient`**

In `inMemoryKvClient()`, add a lists map next to `map`/`sets`:

```ts
  const lists = new Map<string, string[]>();
```

Add these methods to the returned object (after `mget`):

```ts
    async lpush(key, value) {
      const arr = lists.get(key) ?? [];
      arr.unshift(value);
      lists.set(key, arr);
    },
    async lrange(key, start, stop) {
      const arr = lists.get(key) ?? [];
      const end = stop < 0 ? arr.length : stop + 1;
      return arr.slice(start, end);
    },
    async casAppend(headKey, listKey, expectedHead, newHead, entry) {
      const cur = map.has(headKey) ? map.get(headKey)! : null;
      if (cur !== expectedHead) return false;
      const arr = lists.get(listKey) ?? [];
      arr.unshift(entry);
      lists.set(listKey, arr);
      map.set(headKey, newHead);
      return true;
    },
```

- [ ] **Step 5: Implement in `upstashClient`**

In `upstashClient()`, add the Lua script constant next to `CAS_SET`/`CAS_DEL`:

```ts
  // Append atómico del hash-chain (Upstash REST no soporta WATCH).
  const CAS_APPEND = `local cur = redis.call('GET', KEYS[1])
if (ARGV[1] == '' and cur == false) or cur == ARGV[1] then
  redis.call('LPUSH', KEYS[2], ARGV[3])
  redis.call('SET', KEYS[1], ARGV[2])
  return 1
end
return 0`;
```

Add these methods to the returned object (after `mget`):

```ts
    async lpush(key, value) {
      await redis.lpush(key, value);
    },
    async lrange(key, start, stop) {
      return (await redis.lrange<string>(key, start, stop)) ?? [];
    },
    async casAppend(headKey, listKey, expectedHead, newHead, entry) {
      const r = await redis.eval(
        CAS_APPEND,
        [headKey, listKey],
        [expectedHead ?? "", newHead, entry],
      );
      return r === 1;
    },
```

- [ ] **Step 6: Run test to verify it passes**

Run: `pnpm vitest run lib/registry/__tests__/kv-audit.test.ts`
Expected: PASS (2 tests).

- [ ] **Step 7: Commit**

```bash
git add lib/registry/kv.ts lib/registry/__tests__/kv-audit.test.ts
git commit -m "feat(kv): lpush/lrange/casAppend para el hash-chain del audit"
```

---

## Task 2: Tipos + hash-chain puro

**Files:**
- Create: `lib/audit/types.ts`
- Create: `lib/audit/chain.ts`
- Test: `lib/audit/__tests__/chain.test.ts`

**Interfaces:**
- Produces:
  - `AuditActor`, `AuditAction`, `AuditEvent`, `RecordAuditInput` (types.ts)
  - `canonicalize(evt: object): string` — JSON determinístico (claves ordenadas recursivamente)
  - `computeHash(prevHash: string, evt: Omit<AuditEvent, "hash">): string` — sha256 base64url de `prevHash + canonicalize(evt)`
  - `verifyChain(eventsOldestFirst: AuditEvent[]): { ok: boolean; brokenAt?: number }`

- [ ] **Step 1: Write the types**

Create `lib/audit/types.ts`:

```ts
/** Tipos del audit log (spec 2026-09-12). */

export interface AuditActor {
  type: "user" | "ci";
  /** github login, o "ci" si el CI no reportó actor. */
  login: string;
  /** Solo CI: GITHUB_SHA del commit publicado. */
  commit?: string;
  /** Solo CI: "owner/repo" que publicó. */
  repo?: string;
}

export type AuditAction =
  | "publish"
  | "register"
  | "pin"
  | "patch"
  | "delete-miniapp"
  | "delete-version"
  | "set-maintainers"
  | "set-storage-provider"
  | "set-public-key"
  | "scaffold"
  | "seed";

export interface AuditEvent {
  /** 1-based, por cadena. */
  seq: number;
  /** Date.now() al momento del append. */
  ts: number;
  actor: AuditActor;
  action: AuditAction;
  /** miniapp id, o "_global". */
  chain: string;
  miniappId?: string;
  details: Record<string, unknown>;
  /** hash del evento anterior en la cadena ("" para el primero). */
  prevHash: string;
  /** sha256_base64url(prevHash + canonicalize(evento sin `hash`)). */
  hash: string;
}

export interface RecordAuditInput {
  actor: AuditActor;
  action: AuditAction;
  /** miniapp id, o "_global". */
  chain: string;
  miniappId?: string;
  details?: Record<string, unknown>;
}
```

- [ ] **Step 2: Write the failing test**

Create `lib/audit/__tests__/chain.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { canonicalize, computeHash, verifyChain } from "../chain";
import type { AuditEvent } from "../types";

function evt(seq: number, prevHash: string): AuditEvent {
  const base = {
    seq,
    ts: 1000 + seq,
    actor: { type: "user" as const, login: "brian" },
    action: "publish" as const,
    chain: "hellow",
    miniappId: "hellow",
    details: { version: `0.1.${seq}` },
    prevHash,
  };
  return { ...base, hash: computeHash(prevHash, base) };
}

function chainOf(n: number): AuditEvent[] {
  const out: AuditEvent[] = [];
  let prev = "";
  for (let i = 1; i <= n; i++) {
    const e = evt(i, prev);
    out.push(e);
    prev = e.hash;
  }
  return out;
}

describe("canonicalize", () => {
  it("is stable regardless of key insertion order", () => {
    expect(canonicalize({ b: 1, a: { d: 2, c: 3 } })).toBe(
      canonicalize({ a: { c: 3, d: 2 }, b: 1 }),
    );
  });
});

describe("verifyChain", () => {
  it("accepts a valid chain", () => {
    expect(verifyChain(chainOf(3))).toEqual({ ok: true });
  });

  it("detects an edited entry", () => {
    const c = chainOf(3);
    c[1] = { ...c[1], details: { version: "9.9.9" } }; // tamper, hash no recomputado
    expect(verifyChain(c)).toEqual({ ok: false, brokenAt: 2 });
  });

  it("detects a deleted middle entry (broken prevHash link)", () => {
    const c = chainOf(3);
    const gapped = [c[0], c[2]]; // falta seq 2
    expect(verifyChain(gapped)).toEqual({ ok: false, brokenAt: 3 });
  });

  it("empty chain is ok", () => {
    expect(verifyChain([])).toEqual({ ok: true });
  });
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `pnpm vitest run lib/audit/__tests__/chain.test.ts`
Expected: FAIL — cannot find module `../chain`.

- [ ] **Step 4: Implement `lib/audit/chain.ts`**

```ts
/** Hash-chain puro del audit log. Sin I/O. */
import { createHash } from "node:crypto";
import type { AuditEvent } from "./types";

function base64url(buf: Buffer): string {
  return buf.toString("base64").replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
}

/** JSON determinístico: ordena claves recursivamente para que el hash sea estable. */
export function canonicalize(value: unknown): string {
  if (value === null || typeof value !== "object") return JSON.stringify(value) ?? "null";
  if (Array.isArray(value)) return `[${value.map(canonicalize).join(",")}]`;
  const obj = value as Record<string, unknown>;
  const body = Object.keys(obj)
    .sort()
    .map((k) => `${JSON.stringify(k)}:${canonicalize(obj[k])}`)
    .join(",");
  return `{${body}}`;
}

/** hash = sha256_base64url(prevHash + canonicalize(evento sin `hash`)). */
export function computeHash(prevHash: string, evt: Omit<AuditEvent, "hash">): string {
  return base64url(createHash("sha256").update(prevHash + canonicalize(evt)).digest());
}

/**
 * Verifica una cadena ordenada del más viejo al más nuevo: recomputa cada hash y
 * chequea que prevHash apunte al hash anterior. Devuelve el `seq` del primer eslabón roto.
 */
export function verifyChain(eventsOldestFirst: AuditEvent[]): { ok: boolean; brokenAt?: number } {
  let prev = "";
  for (const e of eventsOldestFirst) {
    const { hash, ...rest } = e;
    if (e.prevHash !== prev || computeHash(prev, rest) !== hash) {
      return { ok: false, brokenAt: e.seq };
    }
    prev = hash;
  }
  return { ok: true };
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `pnpm vitest run lib/audit/__tests__/chain.test.ts`
Expected: PASS (5 tests).

- [ ] **Step 6: Commit**

```bash
git add lib/audit/types.ts lib/audit/chain.ts lib/audit/__tests__/chain.test.ts
git commit -m "feat(audit): tipos + hash-chain puro (canonicalize/computeHash/verifyChain)"
```

---

## Task 3: Store del audit (append/lectura/verify)

**Files:**
- Create: `lib/audit/log.ts`
- Test: `lib/audit/__tests__/log.test.ts`

**Interfaces:**
- Consumes: `KvClient` (Task 1), `computeHash`/`verifyChain` (Task 2), `AuditEvent`/`RecordAuditInput` (Task 2)
- Produces:
  - `CHAINS_KEY = "audit:chains"`, `chainListKey(chain)`, `chainHeadKey(chain)`, `GLOBAL_CHAIN = "_global"`
  - `auditStore(client: KvClient)` → `{ record(input), readChain(chain), readAll(filters?), verify() }`
    - `record(input: RecordAuditInput): Promise<AuditEvent>` — append con CAS+retry
    - `readChain(chain: string): Promise<AuditEvent[]>` — newest-first
    - `readAll(filters?: { miniapp?: string; actor?: string; action?: string; limit?: number }): Promise<AuditEvent[]>` — merge de todas las cadenas, newest-first
    - `verify(): Promise<{ ok: boolean; chains: { chain: string; ok: boolean; brokenAt?: number }[] }>`
  - `getAuditStore(): ReturnType<typeof auditStore>` — env-selected (Upstash en prod, in-memory singleton en dev)
  - `recordAudit(input: RecordAuditInput): Promise<void>` — wrapper fire-and-forward (usa `getAuditStore().record`, atrapa y loguea errores)

- [ ] **Step 1: Write the failing test**

Create `lib/audit/__tests__/log.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { inMemoryKvClient } from "../../registry/kv";
import { auditStore } from "../log";
import { verifyChain } from "../chain";

describe("auditStore", () => {
  it("records a chain of events with linked hashes, verifiable", async () => {
    const s = auditStore(inMemoryKvClient());
    await s.record({ actor: { type: "user", login: "brian" }, action: "publish", chain: "hellow", miniappId: "hellow", details: { version: "0.1.1" } });
    await s.record({ actor: { type: "ci", login: "bot" }, action: "publish", chain: "hellow", miniappId: "hellow", details: { version: "0.1.2" } });
    const chain = await s.readChain("hellow");
    expect(chain.map((e) => e.seq)).toEqual([2, 1]); // newest-first
    expect(chain[1].prevHash).toBe(""); // el primero
    expect(chain[0].prevHash).toBe(chain[1].hash); // encadenado
    // verifyChain espera oldest-first
    expect(verifyChain([...chain].reverse())).toEqual({ ok: true });
  });

  it("readAll merges chains newest-first and filters", async () => {
    const s = auditStore(inMemoryKvClient());
    await s.record({ actor: { type: "user", login: "a" }, action: "pin", chain: "x", miniappId: "x", details: {} });
    await s.record({ actor: { type: "user", login: "b" }, action: "publish", chain: "y", miniappId: "y", details: {} });
    const all = await s.readAll();
    expect(all.length).toBe(2);
    expect(all[0].ts).toBeGreaterThanOrEqual(all[1].ts); // newest-first
    expect((await s.readAll({ miniapp: "x" })).every((e) => e.miniappId === "x")).toBe(true);
    expect((await s.readAll({ actor: "b" })).every((e) => e.actor.login === "b")).toBe(true);
    expect((await s.readAll({ action: "pin" })).every((e) => e.action === "pin")).toBe(true);
  });

  it("verify reports ok across chains", async () => {
    const s = auditStore(inMemoryKvClient());
    await s.record({ actor: { type: "user", login: "a" }, action: "seed", chain: "_global", details: { count: 3 } });
    const v = await s.verify();
    expect(v.ok).toBe(true);
    expect(v.chains.find((c) => c.chain === "_global")?.ok).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run lib/audit/__tests__/log.test.ts`
Expected: FAIL — cannot find module `../log`.

- [ ] **Step 3: Implement `lib/audit/log.ts`**

```ts
/** Store del audit log: hash-chain append-only por cadena sobre KV. */
import { type KvClient, inMemoryKvClient, upstashClient } from "../registry/kv";
import { computeHash, verifyChain } from "./chain";
import type { AuditEvent, RecordAuditInput } from "./types";

export const CHAINS_KEY = "audit:chains";
export const GLOBAL_CHAIN = "_global";
export const chainListKey = (chain: string): string => `audit:chain:${chain}`;
export const chainHeadKey = (chain: string): string => `audit:head:${chain}`;

const MAX_RETRIES = 15;
const sleep = (ms: number): Promise<void> => new Promise((r) => setTimeout(r, ms));
const backoffMs = (attempt: number): number => 10 * (attempt + 1) + Math.random() * 15;

export interface AuditFilters {
  miniapp?: string;
  actor?: string;
  action?: string;
  limit?: number;
}

export function auditStore(client: KvClient) {
  return {
    async record(input: RecordAuditInput): Promise<AuditEvent> {
      const listKey = chainListKey(input.chain);
      const headKey = chainHeadKey(input.chain);
      for (let attempt = 0; attempt < MAX_RETRIES; attempt++) {
        const head = await client.get(headKey); // prevHash o null
        const newest = await client.lrange(listKey, 0, 0);
        const prevSeq = newest.length ? (JSON.parse(newest[0]) as AuditEvent).seq : 0;
        const partial = {
          seq: prevSeq + 1,
          ts: Date.now(),
          actor: input.actor,
          action: input.action,
          chain: input.chain,
          miniappId: input.miniappId,
          details: input.details ?? {},
          prevHash: head ?? "",
        };
        const hash = computeHash(partial.prevHash, partial);
        const full: AuditEvent = { ...partial, hash };
        if (await client.casAppend(headKey, listKey, head, hash, JSON.stringify(full))) {
          if (prevSeq === 0) await client.sadd(CHAINS_KEY, input.chain);
          return full;
        }
        await sleep(backoffMs(attempt));
      }
      throw new Error(`audit: no se pudo appendear a la cadena ${input.chain} tras ${MAX_RETRIES} intentos`);
    },

    async readChain(chain: string): Promise<AuditEvent[]> {
      const raws = await client.lrange(chainListKey(chain), 0, -1);
      return raws.map((r) => JSON.parse(r) as AuditEvent);
    },

    async readAll(filters: AuditFilters = {}): Promise<AuditEvent[]> {
      const chains = filters.miniapp ? [filters.miniapp] : await client.smembers(CHAINS_KEY);
      const nested = await Promise.all(chains.map((c) => this.readChain(c)));
      let events = nested.flat().sort((a, b) => b.ts - a.ts || b.seq - a.seq);
      if (filters.actor) events = events.filter((e) => e.actor.login === filters.actor);
      if (filters.action) events = events.filter((e) => e.action === filters.action);
      if (filters.limit && filters.limit > 0) events = events.slice(0, filters.limit);
      return events;
    },

    async verify(): Promise<{ ok: boolean; chains: { chain: string; ok: boolean; brokenAt?: number }[] }> {
      const chains = await client.smembers(CHAINS_KEY);
      const results = await Promise.all(
        chains.map(async (chain) => {
          const oldestFirst = (await this.readChain(chain)).reverse();
          return { chain, ...verifyChain(oldestFirst) };
        }),
      );
      return { ok: results.every((r) => r.ok), chains: results };
    },
  };
}

// Dev usa un in-memory singleton a nivel de módulo (efímero: se pierde al reiniciar el proceso).
// Prod usa Upstash. El registry en dev es fs (jsonStore); el audit en dev es intencionalmente efímero.
let devClient: KvClient | undefined;
function auditKvClient(): KvClient {
  if (process.env.KV_REST_API_URL && process.env.KV_REST_API_TOKEN) return upstashClient();
  devClient ??= inMemoryKvClient();
  return devClient;
}

export function getAuditStore(): ReturnType<typeof auditStore> {
  return auditStore(auditKvClient());
}

/**
 * Registra un evento sin tumbar la operación de negocio: fire-and-forward. Un fallo del audit
 * se loguea pero no se propaga (el audit es secundario a la acción ya cometida).
 */
export async function recordAudit(input: RecordAuditInput): Promise<void> {
  try {
    await getAuditStore().record(input);
  } catch (err) {
    console.error("audit: recordAudit falló", input.action, input.chain, err);
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run lib/audit/__tests__/log.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add lib/audit/log.ts lib/audit/__tests__/log.test.ts
git commit -m "feat(audit): store append-only por-cadena (record/readAll/verify) + recordAudit fire-and-forward"
```

---

## Task 4: Resolver del actor

**Files:**
- Create: `lib/audit/actor.ts`
- Test: `lib/audit/__tests__/actor.test.ts`

**Interfaces:**
- Consumes: `AuditActor` (Task 2)
- Produces:
  - `resolveActor(login: string | null | undefined, ci?: { actor?: string; commit?: string; repo?: string }): AuditActor`
  - **Nota (refinamiento sobre el spec):** el resolver es puro y recibe los datos ya conocidos por la ruta (el `login` de la sesión y, en el upload, los campos del form). Evita re-parsear el body (que se consume una sola vez) y re-invocar `auth()`.

- [ ] **Step 1: Write the failing test**

Create `lib/audit/__tests__/actor.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { resolveActor } from "../actor";

describe("resolveActor", () => {
  it("UI session → user actor", () => {
    expect(resolveActor("brian")).toEqual({ type: "user", login: "brian" });
  });

  it("CI with actor → ci actor con commit/repo", () => {
    expect(resolveActor(null, { actor: "octocat", commit: "abc123", repo: "org/hellow" })).toEqual({
      type: "ci",
      login: "octocat",
      commit: "abc123",
      repo: "org/hellow",
    });
  });

  it("CI sin actor → fallback login 'ci'", () => {
    expect(resolveActor(null, {})).toEqual({ type: "ci", login: "ci" });
    expect(resolveActor(undefined)).toEqual({ type: "ci", login: "ci" });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run lib/audit/__tests__/actor.test.ts`
Expected: FAIL — cannot find module `../actor`.

- [ ] **Step 3: Implement `lib/audit/actor.ts`**

```ts
/** Resuelve el actor de un evento de audit a partir de datos ya conocidos por la ruta. */
import type { AuditActor } from "./types";

/**
 * @param login  github login de la sesión (UI), o null/undefined si la request es del CI (token).
 * @param ci     campos reportados por el CI en el upload (actor/commit/repo). Opcional.
 */
export function resolveActor(
  login: string | null | undefined,
  ci?: { actor?: string; commit?: string; repo?: string },
): AuditActor {
  if (login && login.trim().length > 0) return { type: "user", login: login.trim() };
  const actor: AuditActor = { type: "ci", login: ci?.actor?.trim() || "ci" };
  if (ci?.commit) actor.commit = ci.commit;
  if (ci?.repo) actor.repo = ci.repo;
  return actor;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run lib/audit/__tests__/actor.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add lib/audit/actor.ts lib/audit/__tests__/actor.test.ts
git commit -m "feat(audit): resolveActor (UI login | CI github.actor con fallback)"
```

---

## Task 5: Config — allowlist de admins

**Files:**
- Modify: `lib/config.ts`
- Test: `lib/__tests__/config-audit.test.ts` (create)

**Interfaces:**
- Produces: `auditAdminLogins(): string[]` — CSV de `AUDIT_ADMIN_LOGINS`, trim, sin vacíos (mismo shape que `scaffoldAllowedLogins`).

- [ ] **Step 1: Write the failing test**

Create `lib/__tests__/config-audit.test.ts`:

```ts
import { describe, it, expect, afterEach } from "vitest";
import { auditAdminLogins } from "../config";

afterEach(() => {
  delete process.env.AUDIT_ADMIN_LOGINS;
});

describe("auditAdminLogins", () => {
  it("parses CSV, trims, drops empties", () => {
    process.env.AUDIT_ADMIN_LOGINS = " brian, ,octocat ";
    expect(auditAdminLogins()).toEqual(["brian", "octocat"]);
  });

  it("empty when unset (fail-closed)", () => {
    expect(auditAdminLogins()).toEqual([]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run lib/__tests__/config-audit.test.ts`
Expected: FAIL — `auditAdminLogins is not exported`.

- [ ] **Step 3: Implement in `lib/config.ts`**

Add after `scaffoldAllowedLogins`:

```ts
/**
 * GitHub usernames con acceso al audit log global (`/audit`, `/api/audit` sin miniapp,
 * `/api/audit/verify`), desde `AUDIT_ADMIN_LOGINS` (CSV). Vacío → fail-closed (nadie).
 */
export function auditAdminLogins(): string[] {
  return (process.env.AUDIT_ADMIN_LOGINS ?? "")
    .split(",")
    .map((s) => s.trim())
    .filter((s) => s.length > 0);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run lib/__tests__/config-audit.test.ts`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add lib/config.ts lib/__tests__/config-audit.test.ts
git commit -m "feat(config): auditAdminLogins (AUDIT_ADMIN_LOGINS)"
```

---

## Task 6: Wire `recordAudit` en el publish (upload)

**Files:**
- Modify: `app/api/miniapps/[id]/upload/route.ts`
- Test: `app/api/__tests__/upload-route.test.ts` (extend)

**Interfaces:**
- Consumes: `recordAudit` (Task 3), `resolveActor` (Task 4)
- El upload acepta un actor UI (sesión allowlisted) o CI (Bearer token). Para el CI, lee del form los campos `actor`/`commit`/`repo`.

- [ ] **Step 1: Write the failing test**

Extend `app/api/__tests__/upload-route.test.ts` — agregá un test que verifique que un publish exitoso registra un evento `publish`. Seguí el patrón de mocks del archivo (mock de `getStore` con `getApp/getAll/mutateApp`). Añadí un mock de `@/lib/audit/log`:

```ts
import { vi, describe, it, expect, beforeEach } from "vitest";

const recordAudit = vi.fn();
vi.mock("@/lib/audit/log", () => ({ recordAudit: (...a: unknown[]) => recordAudit(...a) }));

// ... (mocks existentes de getStore/getStorage/auth como en el resto del archivo)

describe("upload route — audit", () => {
  beforeEach(() => recordAudit.mockClear());

  it("registra un evento publish tras un upload exitoso (CI con actor)", async () => {
    // Armá el FormData como los tests existentes de este archivo: file(zip), version, platform,
    // y sumá actor/commit/repo. Autorizá vía Bearer PUBLISH_TOKEN (patrón del archivo).
    // ... construir req y llamar POST(req, { params }) ...
    // expect status 201
    expect(recordAudit).toHaveBeenCalledTimes(1);
    const arg = recordAudit.mock.calls[0][0];
    expect(arg).toMatchObject({
      action: "publish",
      chain: expect.any(String),
      miniappId: expect.any(String),
      actor: { type: "ci", login: "octocat", commit: "sha1", repo: "org/hellow" },
    });
    expect(arg.details).toMatchObject({ version: expect.any(String), signed: expect.any(Boolean) });
  });
});
```

> **Nota para el implementador:** reusá el andamiaje de mocks/`FormData` ya presente en `upload-route.test.ts` (no lo re-inventes). Solo agregá el mock de audit, los campos `actor/commit/repo` al form, y las aserciones sobre `recordAudit`.

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run app/api/__tests__/upload-route.test.ts -t "audit"`
Expected: FAIL — `recordAudit` no fue llamado (0 veces).

- [ ] **Step 3: Add imports**

En `app/api/miniapps/[id]/upload/route.ts`, agregá a los imports:

```ts
import { recordAudit } from "@/lib/audit/log";
import { resolveActor } from "@/lib/audit/actor";
```

- [ ] **Step 4: Leer actor/commit/repo del form**

Cerca de donde se leen los otros `form.get(...)` (después de `const platform = ...` en la línea ~86), agregá:

```ts
    const ciActor = {
      actor: typeof form.get("actor") === "string" ? (form.get("actor") as string) : undefined,
      commit: typeof form.get("commit") === "string" ? (form.get("commit") as string) : undefined,
      repo: typeof form.get("repo") === "string" ? (form.get("repo") as string) : undefined,
    };
```

Para el login de la sesión (flujo UI), obtenelo de forma lazy antes del `recordAudit` (no re-autoriza — `authorizeUpload` ya corrió):

- [ ] **Step 5: Registrar el evento tras el publish exitoso**

Justo antes del `return NextResponse.json({ id, version, url, platform }, { status: 201 });` (línea ~217), agregá:

```ts
    const { auth } = await import("@/auth");
    const session = await auth().catch(() => null);
    await recordAudit({
      actor: resolveActor(session?.githubLogin, ciActor),
      action: "publish",
      chain: id,
      miniappId: id,
      details: { version, platform, integrity, signed: signature !== undefined },
    });
```

> `integrity` y `signature` ya existen como variables en el scope de la función (se calculan antes del `publishVersion`). Si el nombre difiere, usá el valor real del integrity computado y `signed: <hay firma>`.

- [ ] **Step 6: Run test to verify it passes**

Run: `pnpm vitest run app/api/__tests__/upload-route.test.ts`
Expected: PASS (incl. el test nuevo de audit).

- [ ] **Step 7: Commit**

```bash
git add app/api/miniapps/[id]/upload/route.ts app/api/__tests__/upload-route.test.ts
git commit -m "feat(audit): registrar evento publish en upload (actor CI = github.actor)"
```

---

## Task 7: Wire `recordAudit` en las rutas de gestión

**Files:**
- Modify: `app/api/miniapps/[id]/pin/route.ts`
- Modify: `app/api/miniapps/[id]/route.ts` (DELETE + PATCH)
- Modify: `app/api/miniapps/[id]/versions/[version]/route.ts`
- Modify: `app/api/miniapps/[id]/maintainers/route.ts`
- Modify: `app/api/miniapps/[id]/storage-provider/route.ts`
- Modify: `app/api/miniapps/[id]/public-key/route.ts`
- Modify: `app/api/miniapps/[id]/register/route.ts` (o donde viva `register`)
- Modify: `app/api/miniapps/[id]/scaffold/route.ts` (o la ruta de scaffold)
- Modify: `app/api/miniapps/seed/route.ts` (o la ruta de seed)
- Test: `app/api/__tests__/pin-route.test.ts` (extend) + `maintainers-route.test.ts` (extend)

**Interfaces:**
- Consumes: `recordAudit` (Task 3), `resolveActor` (Task 4)
- **Patrón único (todas las rutas UI):** ya tenés `session` (de `auth()`) y el `rec`/resultado de la mutación. Tras la mutación exitosa, antes del `return`, llamá:
  ```ts
  await recordAudit({
    actor: resolveActor(session?.githubLogin),
    action: "<action>",
    chain: id,          // o GLOBAL_CHAIN para seed
    miniappId: id,      // omitir para seed
    details: { /* ver tabla del spec */ },
  });
  ```

- [ ] **Step 1: Write the failing tests (pin + maintainers)**

En `app/api/__tests__/pin-route.test.ts`, agregá el mock de audit y un test:

```ts
const recordAudit = vi.fn();
vi.mock("@/lib/audit/log", () => ({ recordAudit: (...a: unknown[]) => recordAudit(...a) }));
// ...
it("registra un evento pin tras pinear", async () => {
  recordAudit.mockClear();
  // ... llamar PUT con version válida y sesión admin (patrón del archivo) ...
  expect(recordAudit).toHaveBeenCalledWith(
    expect.objectContaining({ action: "pin", miniappId: expect.any(String), details: expect.objectContaining({ to: expect.anything() }) }),
  );
});
```

Hacé lo análogo en `maintainers-route.test.ts` para `action: "set-maintainers"` con `details.added/removed`.

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm vitest run app/api/__tests__/pin-route.test.ts app/api/__tests__/maintainers-route.test.ts -t "registra un evento"`
Expected: FAIL — `recordAudit` no llamado.

- [ ] **Step 3: Instrumentar `pin`**

En `app/api/miniapps/[id]/pin/route.ts`, imports:

```ts
import { recordAudit } from "@/lib/audit/log";
import { resolveActor } from "@/lib/audit/actor";
```

Capturá el pin anterior antes de mutar (para `from`): `const fromPin = rec?.pinnedVersion ?? null;` (usá el campo real del record; si no existe, omití `from`). Tras `const next = await store.mutateApp(...)` y antes del `return`:

```ts
    await recordAudit({
      actor: resolveActor(session?.githubLogin),
      action: "pin",
      chain: id,
      miniappId: id,
      details: { from: fromPin, to: version },
    });
```

- [ ] **Step 4: Instrumentar el resto (mismo patrón)**

Aplicá el patrón único en cada ruta con su `action`/`details` de la tabla del spec:

| Ruta | `action` | `details` |
|---|---|---|
| `[id]` DELETE | `delete-miniapp` | `{ versions: <nº de versiones que tenía rec> }` |
| `[id]` PATCH | `patch` | `{ fields: Object.keys(<body validado>) }` |
| `versions/[version]` DELETE | `delete-version` | `{ version }` |
| `maintainers` | `set-maintainers` | `{ added: string[], removed: string[] }` (diff antes/después) |
| `storage-provider` | `set-storage-provider` | `{ provider }` |
| `public-key` | `set-public-key` | `{ hasKey: <la request setea una key no vacía> }` |
| `register` | `register` | `{}` |
| `scaffold` | `scaffold` | `{ template?, repo? }` (lo que devuelva el scaffolder) |
| `seed` | `seed` | `{ count: <nº seedeado> }` → `chain: GLOBAL_CHAIN`, sin `miniappId` |

> Para `seed`, importá `GLOBAL_CHAIN` de `@/lib/audit/log` y usá `chain: GLOBAL_CHAIN`.
> Para acciones que borran la miniapp (`delete-miniapp`), registrá **antes** de borrar si necesitás datos del `rec` (ej. `versions`), pero llamá `recordAudit` **después** de confirmar que la mutación borró (para no auditar un borrado que no ocurrió). Capturá `const versions = Object.keys(rec?.versions ?? {}).length;` antes, y `recordAudit` tras el `mutateApp` exitoso.

> **Si no encontrás una ruta exacta** (register/scaffold/seed pueden vivir en otra subruta), buscala con `codegraph_search` o `grep -rl "seed\|scaffold\|register"` en `app/api/` antes de instrumentar. Instrumentá solo mutaciones reales.

- [ ] **Step 5: Run tests to verify they pass**

Run: `pnpm vitest run app/api/__tests__/pin-route.test.ts app/api/__tests__/maintainers-route.test.ts`
Expected: PASS.

- [ ] **Step 6: Run the full route test suite (no regressions)**

Run: `pnpm vitest run app/api/__tests__`
Expected: PASS (todas).

- [ ] **Step 7: Commit**

```bash
git add app/api/miniapps
git commit -m "feat(audit): registrar mutaciones de gestión (pin/delete/patch/maintainers/storage/public-key/register/scaffold/seed)"
```

---

## Task 8: API — `GET /api/audit` y `GET /api/audit/verify`

**Files:**
- Create: `app/api/audit/route.ts`
- Create: `app/api/audit/verify/route.ts`
- Test: `app/api/__tests__/audit-route.test.ts`

**Interfaces:**
- Consumes: `getAuditStore` (Task 3), `auditAdminLogins` (Task 5), `canScaffold`/`canManageMiniapp` (existentes), `getStore` (para `rec.maintainers` del check por-miniapp)
- Produces:
  - `GET /api/audit?miniapp=&actor=&action=&limit=` → `{ events: AuditEvent[] }`. Sin `miniapp` → solo admin. Con `miniapp` → admin o maintainer de esa miniapp.
  - `GET /api/audit/verify` → `{ ok, chains }`. Solo admin.

- [ ] **Step 1: Write the failing test**

Create `app/api/__tests__/audit-route.test.ts`:

```ts
import { vi, describe, it, expect, beforeEach } from "vitest";

const store = { record: vi.fn(), readChain: vi.fn(), readAll: vi.fn(), verify: vi.fn() };
vi.mock("@/lib/audit/log", () => ({
  getAuditStore: () => store,
  GLOBAL_CHAIN: "_global",
}));
const authMock = vi.fn();
vi.mock("@/auth", () => ({ auth: () => authMock() }));
vi.mock("@/lib/config", async (orig) => ({
  ...(await orig<typeof import("@/lib/config")>()),
  auditAdminLogins: () => ["brian"],
}));
const getApp = vi.fn();
vi.mock("@/lib/registry/store", () => ({ getStore: () => ({ getApp }) }));

import { GET } from "@/app/api/audit/route";

beforeEach(() => {
  store.readAll.mockReset().mockResolvedValue([{ seq: 1, action: "publish" }]);
  authMock.mockReset();
  getApp.mockReset();
});

describe("GET /api/audit", () => {
  it("feed global exige admin (401 sin sesión admin)", async () => {
    authMock.mockResolvedValue({ githubLogin: "rando" });
    const res = await GET(new Request("http://x/api/audit"));
    expect(res.status).toBe(401);
  });

  it("admin ve el feed global", async () => {
    authMock.mockResolvedValue({ githubLogin: "brian" });
    const res = await GET(new Request("http://x/api/audit"));
    expect(res.status).toBe(200);
    expect(await res.json()).toEqual({ events: [{ seq: 1, action: "publish" }] });
  });

  it("por-miniapp: maintainer no-admin ve su cadena", async () => {
    authMock.mockResolvedValue({ githubLogin: "rando" });
    getApp.mockResolvedValue({ maintainers: ["rando"] });
    const res = await GET(new Request("http://x/api/audit?miniapp=hellow"));
    expect(res.status).toBe(200);
    expect(store.readAll).toHaveBeenCalledWith(expect.objectContaining({ miniapp: "hellow" }));
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run app/api/__tests__/audit-route.test.ts`
Expected: FAIL — cannot find module `@/app/api/audit/route`.

- [ ] **Step 3: Implement `app/api/audit/route.ts`**

```ts
import { NextResponse } from "next/server";
import { getAuditStore } from "@/lib/audit/log";
import { getStore } from "@/lib/registry/store";
import { auditAdminLogins } from "@/lib/config";
import { canScaffold, canManageMiniapp } from "@/lib/scaffold-authz";
import { AuthError } from "@/lib/auth";
import { errorBody, statusForError } from "@/lib/http";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

/**
 * GET /api/audit?miniapp=&actor=&action=&limit= — feed del audit log.
 * Sin `miniapp`: feed global (solo admins). Con `miniapp`: admin o maintainer de esa miniapp.
 */
export async function GET(req: Request): Promise<NextResponse> {
  try {
    const url = new URL(req.url);
    const miniapp = url.searchParams.get("miniapp") ?? undefined;
    const actor = url.searchParams.get("actor") ?? undefined;
    const action = url.searchParams.get("action") ?? undefined;
    const limitRaw = url.searchParams.get("limit");
    const limit = limitRaw ? Number(limitRaw) : undefined;

    const { auth } = await import("@/auth");
    const session = await auth();
    const login = session?.githubLogin;
    const admins = auditAdminLogins();

    if (miniapp) {
      const rec = await getStore().getApp(miniapp);
      if (!canManageMiniapp(login, rec?.maintainers, admins)) throw new AuthError();
    } else if (!canScaffold(login, admins)) {
      throw new AuthError();
    }

    const events = await getAuditStore().readAll({ miniapp, actor, action, limit });
    return NextResponse.json({ events }, { status: 200 });
  } catch (err) {
    return NextResponse.json(errorBody(err), { status: statusForError(err) });
  }
}
```

- [ ] **Step 4: Implement `app/api/audit/verify/route.ts`**

```ts
import { NextResponse } from "next/server";
import { getAuditStore } from "@/lib/audit/log";
import { auditAdminLogins } from "@/lib/config";
import { canScaffold } from "@/lib/scaffold-authz";
import { AuthError } from "@/lib/auth";
import { errorBody, statusForError } from "@/lib/http";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

/** GET /api/audit/verify — verifica la integridad de todas las cadenas. Solo admins. */
export async function GET(): Promise<NextResponse> {
  try {
    const { auth } = await import("@/auth");
    const session = await auth();
    if (!canScaffold(session?.githubLogin, auditAdminLogins())) throw new AuthError();
    const result = await getAuditStore().verify();
    return NextResponse.json(result, { status: 200 });
  } catch (err) {
    return NextResponse.json(errorBody(err), { status: statusForError(err) });
  }
}
```

- [ ] **Step 5: Verify `AuthError` maps to 401**

Confirmá que `statusForError` en `lib/http.ts` mapea `AuthError` (code `UNAUTHORIZED`) a 401. Si no, agregá el mapeo (seguí el patrón de `ConflictError`→409 ya presente).

Run: `pnpm vitest run app/api/__tests__/audit-route.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 6: Commit**

```bash
git add app/api/audit lib/http.ts app/api/__tests__/audit-route.test.ts
git commit -m "feat(audit): GET /api/audit (admin|maintainer) + /api/audit/verify (admin)"
```

---

## Task 9: Página `/audit` (admins)

**Files:**
- Create: `app/audit/page.tsx`
- Create: `app/audit/audit-table.tsx`

**Interfaces:**
- Consumes: `getAuditStore` (Task 3), `auditAdminLogins` (Task 5), `canScaffold`
- Modela el patrón de `app/metrics/page.tsx` (server component, `dynamic="force-dynamic"`) + gating admin.

- [ ] **Step 1: Implement the page (server component, admin-gated)**

Create `app/audit/page.tsx`:

```tsx
import { getAuditStore } from "@/lib/audit/log";
import { auditAdminLogins } from "@/lib/config";
import { canScaffold } from "@/lib/scaffold-authz";
import { AuditTable } from "./audit-table";

export const dynamic = "force-dynamic";

export default async function AuditPage() {
  const { auth } = await import("@/auth");
  const session = await auth();
  if (!canScaffold(session?.githubLogin, auditAdminLogins())) {
    return (
      <main className="page">
        <h1 className="page-title">Auditoría</h1>
        <p role="status" className="empty">No autorizado. Esta página es solo para administradores.</p>
      </main>
    );
  }

  const store = getAuditStore();
  const [events, integrity] = await Promise.all([store.readAll({ limit: 500 }), store.verify()]);

  return (
    <main className="page">
      <p className="eyebrow">Seguridad</p>
      <h1 className="page-title">Auditoría</h1>
      <p className="page-lede">Quién hizo qué en el control-plane: publicaciones y cambios de gestión.</p>

      {integrity.ok ? (
        <p role="status" className="audit-integrity ok">✅ Cadenas íntegras ({integrity.chains.length}).</p>
      ) : (
        <p role="alert" className="audit-integrity bad">
          ⚠️ Integridad comprometida:{" "}
          {integrity.chains.filter((c) => !c.ok).map((c) => `${c.chain} (seq ${c.brokenAt})`).join(", ")}
        </p>
      )}

      <AuditTable events={events} />
    </main>
  );
}
```

- [ ] **Step 2: Implement the client table (filtros)**

Create `app/audit/audit-table.tsx`:

```tsx
"use client";
import { useMemo, useState } from "react";
import type { AuditEvent } from "@/lib/audit/types";

export function AuditTable({ events }: { events: AuditEvent[] }) {
  const [action, setAction] = useState("");
  const [actor, setActor] = useState("");
  const actions = useMemo(() => [...new Set(events.map((e) => e.action))].sort(), [events]);
  const filtered = events.filter(
    (e) => (!action || e.action === action) && (!actor || e.actor.login.includes(actor)),
  );

  return (
    <section className="audit-section">
      <div className="audit-filters">
        <label>
          Acción{" "}
          <select value={action} onChange={(e) => setAction(e.target.value)}>
            <option value="">todas</option>
            {actions.map((a) => (
              <option key={a} value={a}>{a}</option>
            ))}
          </select>
        </label>
        <label>
          Actor <input value={actor} onChange={(e) => setActor(e.target.value)} placeholder="login" />
        </label>
        <span className="audit-count">{filtered.length} eventos</span>
      </div>

      {filtered.length === 0 ? (
        <p role="status" className="empty">Sin eventos.</p>
      ) : (
        <table className="audit-log">
          <thead>
            <tr><th>Cuándo</th><th>Actor</th><th>Acción</th><th>Miniapp</th><th>Detalle</th></tr>
          </thead>
          <tbody>
            {filtered.map((e) => (
              <tr key={`${e.chain}:${e.seq}`}>
                <td>{new Date(e.ts).toISOString().replace("T", " ").slice(0, 19)}</td>
                <td>
                  <code>{e.actor.login}</code>
                  <span className={`actor-badge ${e.actor.type}`}>{e.actor.type}</span>
                </td>
                <td><code>{e.action}</code></td>
                <td>{e.miniappId ?? "—"}</td>
                <td><code className="audit-details">{JSON.stringify(e.details)}</code></td>
              </tr>
            ))}
          </tbody>
        </table>
      )}
    </section>
  );
}
```

- [ ] **Step 3: Verify it builds**

Run: `pnpm build`
Expected: build OK (compila la página `/audit`). Si hay clases CSS ausentes no rompe el build (Tailwind/global CSS es best-effort visual); el objetivo del step es que compile.

- [ ] **Step 4: Commit**

```bash
git add app/audit
git commit -m "feat(audit): página /audit (admin) con feed, filtros y banner de integridad"
```

---

## Task 10: Panel "Historial" en el detalle de la miniapp

**Files:**
- Modify: `app/miniapp/[id]/page.tsx`

**Interfaces:**
- Consumes: `getAuditStore` (Task 3), `auditAdminLogins`, `canManageMiniapp`, `AuditTable` (Task 9)
- Muestra la cadena de esa miniapp a sus maintainers (o admins). Reusa `AuditTable`.

- [ ] **Step 1: Read the current detail page**

Leé `app/miniapp/[id]/page.tsx` completo para ubicar dónde ya se resuelve `id`, el `rec`/detail y la sesión (si la hay). Seguí su estructura de secciones.

- [ ] **Step 2: Add the Historial section (maintainer/admin-gated)**

Dentro del componente de la página, tras obtener `id` y el record, agregá:

```tsx
  const { auth } = await import("@/auth");
  const session = await auth();
  const rec = await getStore().getApp(id); // si la página ya lo tiene, reusá esa variable
  const canSeeAudit = canManageMiniapp(session?.githubLogin, rec?.maintainers, auditAdminLogins());
  const history = canSeeAudit ? await getAuditStore().readAll({ miniapp: id, limit: 200 }) : [];
```

Y en el JSX, donde encaje entre las secciones existentes:

```tsx
  {canSeeAudit && (
    <section className="miniapp-section">
      <h2>Historial</h2>
      <p className="page-lede">Publicaciones y cambios de gestión de esta miniapp.</p>
      <AuditTable events={history} />
    </section>
  )}
```

Agregá los imports correspondientes (`getAuditStore`, `auditAdminLogins`, `canManageMiniapp`, `AuditTable`, y `getStore` si no estaba).

- [ ] **Step 3: Verify it builds**

Run: `pnpm build`
Expected: build OK.

- [ ] **Step 4: Commit**

```bash
git add app/miniapp/[id]/page.tsx
git commit -m "feat(audit): panel Historial por-miniapp (maintainers/admin) en el detalle"
```

---

## Task 11: Template — el CI publica su `github.actor`

**Files:**
- Modify: `../miniapp-template/scripts/publish.mjs`

**Interfaces:**
- El script de publish suma al request de upload los campos `actor`/`commit`/`repo` desde las env-vars auto de GitHub Actions. `scripts/*.mjs` es **template-owned** → se propaga por template-sync.

- [ ] **Step 1: Read the current publish.mjs**

Leé `../miniapp-template/scripts/publish.mjs` para ubicar dónde arma el `FormData`/multipart del upload (donde ya agrega `file`, `version`, `manifest`, `signature`, etc.).

- [ ] **Step 2: Append the actor fields to the upload body**

Donde se arman los campos del form, agregá (usando la API de FormData que ya use el script):

```js
// Atribución del audit log (Backstage): quién disparó este publish en CI.
// GITHUB_ACTOR / GITHUB_SHA / GITHUB_REPOSITORY son env-vars auto de GitHub Actions.
if (process.env.GITHUB_ACTOR) form.append("actor", process.env.GITHUB_ACTOR);
if (process.env.GITHUB_SHA) form.append("commit", process.env.GITHUB_SHA);
if (process.env.GITHUB_REPOSITORY) form.append("repo", process.env.GITHUB_REPOSITORY);
```

(Adaptá `form.append` al nombre real de la variable del form en el script.)

- [ ] **Step 3: Verify (dry-run / lint)**

Run: `node --check ../miniapp-template/scripts/publish.mjs`
Expected: sin errores de sintaxis.

- [ ] **Step 4: Commit (en el repo del template)**

```bash
git -C ../miniapp-template add scripts/publish.mjs
git -C ../miniapp-template commit -m "feat(publish): mandar github.actor/commit/repo para el audit log de Backstage"
```

> El push/PR del template y su propagación por template-sync los maneja el flujo de finishing (fuera de este plan de código). Dejá el commit local.

---

## Task 12: ADR + docs (docs-sync)

**Files:**
- Create: `memory-bank/bolts/audit-log/adr-016.md`
- Modify: `docs/API-REFERENCE.md`, `docs/PLATFORM-OVERVIEW.md`, `docs/GLOSARIO.md`
- Create: `docs/AUDIT-LOG.md` (+ wire en `lib/docs/nav.ts`)

**Interfaces:**
- Consumes: la doc del sitio se alimenta de `docs/*.md` vía `lib/docs/nav.ts`.

- [ ] **Step 1: Write ADR-016**

Create `memory-bank/bolts/audit-log/adr-016.md` con **Context / Decision / Consequences**: el problema (sin trazabilidad de quién cambió qué), la decisión (hash-chain append-only por cadena, atribución por github.actor, acceso admin/maintainer), y las consecuencias — incluyendo explícitamente que es tamper-**evidente**, no tamper-**proof** (un admin con acceso total a KV puede reescribir una cadena; el sink externo quedó fuera de alcance).

- [ ] **Step 2: Document the API**

En `docs/API-REFERENCE.md`, agregá la sección de los endpoints nuevos: `GET /api/audit` (params + auth: admin | maintainer con `miniapp`) y `GET /api/audit/verify` (admin). Y en el `POST .../upload`, documentá los campos opcionales `actor`/`commit`/`repo` que el CI puede mandar para atribución. Verificá los shapes contra el código real (Tasks 6 y 8) — no inventes.

- [ ] **Step 3: Platform overview + glosario**

En `docs/PLATFORM-OVERVIEW.md`, sumá un párrafo en la sección de seguridad/observabilidad sobre el audit log (qué registra, quién lo ve, tamper-evidencia). En `docs/GLOSARIO.md`, agregá entradas: **audit log**, **hash-chain**, **tamper-evidente**.

- [ ] **Step 4: New doc page + nav**

Create `docs/AUDIT-LOG.md` (qué es, qué eventos registra, atribución CI vs UI, acceso, cómo verificar la integridad, la limitación tamper-evidente). Cablealo en `lib/docs/nav.ts` (grupo apropiado, ej. "Seguridad" u "Observabilidad" — mirá los grupos existentes; si no hay uno de seguridad, sumalo al que mejor encaje) con `slug: "audit-log"`, `title: "Audit log"` y un blurb.

- [ ] **Step 5: Verify the site builds with the docs**

Run: `pnpm build`
Expected: build OK — la nueva doc renderiza y aparece en la nav.

- [ ] **Step 6: Commit**

```bash
git add memory-bank/bolts/audit-log/adr-016.md docs/API-REFERENCE.md docs/PLATFORM-OVERVIEW.md docs/GLOSARIO.md docs/AUDIT-LOG.md lib/docs/nav.ts
git commit -m "docs(audit): ADR-016 + API-REFERENCE + página AUDIT-LOG + glosario/overview"
```

---

## Cierre

Tras completar las 12 tasks:

- [ ] Correr la suite completa: `pnpm test` (o `pnpm vitest run`) + `pnpm build`. Todo verde.
- [ ] **REQUIRED SUB-SKILL:** Usar superpowers:finishing-a-development-branch para verificar tests y elegir merge/PR.
- [ ] Antes de activar en prod: setear `AUDIT_ADMIN_LOGINS` en Vercel (Production **y** Preview) con los logins admin (ej. `DentVega`). Sin eso, `/audit` y el feed global quedan fail-closed (nadie entra) — que es el default seguro.

## Self-review (hecho al escribir el plan)

- **Cobertura del spec:** eventos+details (Tasks 6-7), actor CI/UI (Tasks 4, 6, 11), hash-chain+verify (Tasks 2-3), acceso admin/maintainer (Tasks 5, 8-10), fire-and-forward (Task 3), retención sin LTRIM (por diseño, no hay borrado), docs+ADR (Task 12). ✔
- **Consistencia de tipos:** `AuditEvent`/`AuditActor`/`RecordAuditInput` definidos en Task 2 y usados igual en 3/6/7/8/9/10. `casAppend`/`lpush`/`lrange` definidos en Task 1 y consumidos en 3. `resolveActor(login, ci?)` definido en 4 y usado en 6-7. ✔
- **Sin placeholders:** cada step de código trae el código real. Las rutas cuya ubicación exacta no verifiqué (register/scaffold/seed) llevan instrucción explícita de localizarlas antes de instrumentar. ✔
